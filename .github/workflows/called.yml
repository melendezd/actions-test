name: called

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  called-job:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - run: env
    
